<h1>Manage Users</h1>

<p>Number of users: {{users.length}}</p> <!-- Add this -->

<ul>
  {{#each users}}
    <li>{{this.name}} ({{this.email}})</li>
  {{/each}}
</ul>
<a href="/admin">Back to Dashboard</a>
